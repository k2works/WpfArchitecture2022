# 作業履歴 2023-01-18

## 概要

2023-01-18の作業内容をまとめています。

## コミット: 269f10a

### メッセージ

```
起動スクリプトの修正
```

### 変更されたファイル

- M	Start-Dev.ps1

### 変更内容

```diff
commit 269f10a0e653a28c9f131a5e3e60b25c40e83473
Author: Atsushi Nakamura <16175660+nuitsjp@users.noreply.github.com>
Date:   Wed Jan 18 06:53:24 2023 +0900

    起動スクリプトの修正

diff --git a/Start-Dev.ps1 b/Start-Dev.ps1
index 88e8164..782db0f 100644
--- a/Start-Dev.ps1
+++ b/Start-Dev.ps1
@@ -3,10 +3,36 @@
 # docker start adventureworks
 docker-compose -f (Join-Path $PSScriptRoot 'compose-dev.yaml') up -d
 
+## データベースへの接続
+$connectionString = 'Data Source=localhost;Initial Catalog=AdventureWorks;User ID=sa;Password=P@ssw0rd!'
+
+$user = [System.Security.Principal.WindowsIdentity]::GetCurrent().Name
+$sqlQuery = "
+update 
+    HumanResources.Employee 
+set 
+    LoginID = '$user' 
+where 
+    BusinessEntityID = 260;
+
+select 'Updated login user.'"
+
+while ($true) {
+    $connection = New-Object System.Data.SQLClient.SQLConnection $connectionString
+    $sqlCommand = New-Object System.Data.SQLClient.SQLCommand($sqlQuery, $connection)
+    try {
+        $connection.Open()
+        
+        Write-Host $sqlCommand.ExecuteScalar()    
+        break
+    }
+    catch {
+        Write-Host "SQL Server 起動待機..."
+        Start-Sleep -Seconds 1
+    }
+    finally {
+        $connection.Dispose()
+        $sqlCommand.Dispose()
+    }
+}
 
-# update
-# 	HumanResources.Employee
-# set
-# 	LoginID = 'DESKTOP-AUA6P9K\atsus'
-# where
-# 	BusinessEntityID = 260
\ No newline at end of file

```

